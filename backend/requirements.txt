# UDM Fork - Sky Quality Meter backend dependencies
# Tested with Python 3.10 / 3.11 / 3.12 on Linux

# Web framework
fastapi>=0.110.0,<1.0
uvicorn[standard]>=0.25.0
starlette>=0.36.0
pydantic>=2.6.0
python-multipart>=0.0.9

# Env / config
python-dotenv>=1.0.0

# MongoDB (optional, used for preferences only)
motor>=3.3.0
pymongo>=4.5.0

# Serial / USB discovery
pyserial>=3.5
pyudev>=0.24.0; sys_platform == "linux"

# ESP firmware flashing (used by /api/firmware/flash for DIY SQM ESP8266/ESP32)
esptool>=4.7.0
